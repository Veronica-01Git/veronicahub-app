# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# O projeto usa bun (bun.lock). Lockfile de npm gerado por engano não entra.
package-lock.json

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Intermediários brutos de processamento de asset (rembg/ffmpeg) — nunca
# commitar direto, só as versões otimizadas em public/
assets/processed/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Material de publicação gerado por scripts/render-instagram-card.mjs
out/

# Saída de scripts/roteiro-pdf.mjs. O PDF é derivado do
# ROTEIRO-DE-SUBIDA.md — versionar o derivado significa duas versões do mesmo
# documento podendo divergir, e é o markdown que manda.
tmp/
